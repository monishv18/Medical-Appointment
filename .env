# Environment variables for your FastAPI project
# Modify the values as needed

DATABASE_URL=postgresql://postgres:Monishv@063@localhost:5432/medical_appointment_db 

# Next steps to use this .env file:
# 1. Install required packages:
# pip install sqlalchemy psycopg2-binary python-dotenv
# 2. In your backend/config.py, load this .env file using load_dotenv().
# 3. In backend/database.py, use DATABASE_URL to create engine and SessionLocal.
# 4. Run your FastAPI app and confirm connection to PostgreSQL.
# 5. Ensure PostgreSQL database 'medical_appointment_db' is already created in pgAdmin.